---
description: "Task list for Detailed Session Report implementation"
---

# Tasks: Detailed Session Report

**Input**: Design documents from `/specs/006-detailed-session-report/`
**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/session-report-ui.md

**Organization**: Tasks are grouped by user story to enable independent implementation and testing of each story.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Project initialization and basic structure components

- [x] T001 Create session components directory in `frontend/src/components/session/`
- [x] T002 Generate print styling layout shell and PDF export/print button in `frontend/src/app/session/[id]/page.tsx`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core infrastructure that MUST be complete before ANY user story can be implemented

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [x] T003 Define `HierarchicalReport` and `ReportPrintProps` interfaces within a shared context in `frontend/src/app/session/[id]/types.ts`
- [x] T004 Build generic tabular `DataGrid` shared component for metric displays in `frontend/src/components/session/DataGrid.tsx`

**Checkpoint**: Foundation ready - user story implementation can now begin in parallel.

---

## Phase 3: User Story 1 - Review Patient's Current Heart State (Model 1) (Priority: P1) 🎯 MVP

**Goal**: Clinicians need viewing access to the comprehensive initial cardiovascular state assessed by Model 1 to determine exactly whether an anomaly (e.g., murmur or arrhythmia) is present.

**Independent Test**: Model 1 outputs (heart state) prominently display with clinical confidence scores alongside the real-time recording data.

### Implementation for User Story 1

- [x] T005 [P] [US1] Create `Model1StateCard` component in `frontend/src/components/session/Model1StateCard.tsx`
- [x] T006 [US1] Integrate `Model1StateCard` into the primary viewport in `frontend/src/app/session/[id]/page.tsx`

**Checkpoint**: At this point, User Story 1 should be fully functional and testable independently.

---

## Phase 4: User Story 2 - Analyze Secondary Functional Modalities (Priority: P2)

**Goal**: Cardiologists must evaluate specific functional parameters (such as murmur severity/timing or supplementary feature analysis) processed by Model 2.

**Independent Test**: Ensure functional predictions correctly populate a dedicated detailed widget.

### Implementation for User Story 2

- [x] T007 [P] [US2] Create secondary `Model2DiagnosticCard` component in `frontend/src/components/session/Model2DiagnosticCard.tsx`
- [x] T008 [US2] Integrate `Model2DiagnosticCard` chronologically below Model 1 in `frontend/src/app/session/[id]/page.tsx`

**Checkpoint**: At this point, User Stories 1 AND 2 should both work independently.

---

## Phase 5: User Story 3 - View Predictive Prognosis (Priority: P3)

**Goal**: Visualizing the patient's extrapolated risk trajectory generated by Model 3 to inform long-term patient care strategies.

**Independent Test**: Display of a "Future Prognosis" or "Risk Assessment" card representing predictive timelines safely separated from live data.

### Implementation for User Story 3

- [x] T009 [P] [US3] Create `Model3PrognosisCard` component in `frontend/src/components/session/Model3PrognosisCard.tsx`
- [x] T010 [US3] Render `Model3PrognosisCard` clearly sectioned off at the bottom of the report flow in `frontend/src/app/session/[id]/page.tsx`

**Checkpoint**: All user stories should now be independently functional.

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Improvements that affect multiple user stories

- [x] T011 [P] Rigorously test print CSS media queries (e.g., `print:hidden`) across layout headers/nav in `frontend/src/app/session/[id]/page.tsx` and global styles.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies - can start immediately
- **Foundational (Phase 2)**: Depends on Setup completion - BLOCKS all user stories
- **User Stories (Phase 3+)**: All depend on Foundational phase completion
- **Polish (Final Phase)**: Depends on all desired user stories being complete

### Implementation Strategy

### Incremental Delivery

1. Complete Setup + Foundational → Foundation ready
2. Add User Story 1 → Test independently → Deploy/Demo (MVP!)
3. Add User Story 2 → Test independently → Deploy/Demo
4. Add User Story 3 → Test independently → Deploy/Demo
5. Each story adds value without breaking previous stories
